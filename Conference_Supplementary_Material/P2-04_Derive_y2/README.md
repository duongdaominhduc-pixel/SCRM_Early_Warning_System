# Phase 2-04: Target Derivation

## 1. Overview
This module is responsible for defining the predictive objective. The `stockout_flag` outcome variables for future temporal instances (W+1 and W+2) are shifted relative to the current temporal instance (W0). This formulates the target labels required for the supervised machine learning models.
